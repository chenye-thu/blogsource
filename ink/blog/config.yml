site:
    title: "夜凉如水"
    subtitle: "人丑就要多读书"
    limit: 10
    theme: theme
    lang: zh
    url: "http://chenye-thu.github.io/"
    comment: username
    logo: "/images/avatar.png"
    # link: "{category}/{year}/{month}/{day}/{title}.html"
    # root: "/blog"

authors:
    me:
        name: "夜凉如水"
        intro: "人丑就要多读书"
        avatar: "/images/icon.png"

build:
    # output: "public"
    port: 8000
    # Copied files to public folder when build
    copy:
        - "source/images"
    # Excuted command when use 'ink publish'
    publish: |
        git add . -A
        git commit -m "update"
        git push origin
